export default function SignOutPage() {
  return (
    <div className="min-h-screen flex items-center justify-center px-4">
      <div className="w-full max-w-md space-y-6 text-center">
        <h1 className="text-2xl font-semibold">Sign out</h1>
        <p className="text-gray-500 mt-2">Authentication isn’t configured yet. This page is a placeholder.</p>
        <p className="text-xs text-gray-500">We’ll enable sign-out once auth is wired.</p>
      </div>
    </div>
  );
}
